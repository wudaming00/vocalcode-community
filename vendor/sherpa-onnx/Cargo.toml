# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "sherpa-onnx"
version = "1.13.6"
build = false
include = [
    "src/**",
    "Cargo.toml",
    "README.md",
    "LICENSE*",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Safe Rust wrapper for sherpa-onnx speech recognition toolkit"
documentation = "https://docs.rs/sherpa-onnx"
readme = "README.md"
keywords = [
    "speech",
    "speech-to-text",
    "stt",
    "onnx",
    "asr",
]
categories = [
    "api-bindings",
    "multimedia::audio",
]
license = "Apache-2.0"
repository = "https://github.com/k2-fsa/sherpa-onnx"

[package.metadata.docs.rs]
default-features = false
features = []

[features]
# VocalCode's reviewed desktop closure is the official shared no-TTS archive.
# Keeping the vendored wrapper's own default aligned prevents
# `cargo test --workspace` from unifying an unintended static feature into the
# application dependency.
default = ["shared"]
shared = ["sherpa-onnx-sys/shared"]
static = ["sherpa-onnx-sys/static"]

[lib]
name = "sherpa_onnx"
path = "src/lib.rs"

[dependencies.serde]
version = "1.0"
features = ["derive"]

[dependencies.serde_json]
version = "1.0"

[dependencies.sherpa-onnx-sys]
version = "1.13.6"
default-features = false
path = "../sherpa-onnx-sys"
