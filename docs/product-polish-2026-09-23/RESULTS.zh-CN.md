# 产品打磨验证记录（本轮完成）

开始：2026-09-23 12:24:49 UTC；五小时主体工作于 17:25:37 UTC 完成，随后整理交付清单。
**本轮本地开发、研究和下述验证已完成；未覆盖当前安装，也未发布。未验证项在文末单列。**

以上为五小时任务交付时的状态。用户随后另行要求本机替换及更小的常驻胶囊，后续变化见 [小胶囊迭代](TINY-CAPSULE.zh-CN.md)；不追溯修改本轮实验数据。

工作仓库为公开社区版 `vocalcode-community-public-20260922-v2`。
保留了开始前的后台听写、输入保护及测试改动，没有改写其他项目。
本轮未安装、覆盖、签名、发布、推送或更新用户当前的 VocalCode / 竞品 / CLI。

## 已实现

- Windows 可选常驻深色小控件：免提开始、停止、取消、历史、设置、隐藏一小时、三边停靠；空闲全屏不打扰。
- 与原有输入队列共用优先级，状态代次和 750 ms 有效期限制旧请求。取消 / 退出 / 强制停止清除等待请求；重复点击、队列满不会重新开始录音。
- 输入失败的文字恢复提示只指向已有历史，不把私人原文传给小控件，不重试输入，也不声称“没有任何文字写入”。
- 修复旧指示器的点击穿透、非激活、任务栏样式、圆角与 Windows 文字放大裁切。
- 隐藏旧指示器不再接收音量，重复数值去重；新增历史条目的窄屏布局调整。
- 可选智能改写：本地 Ollama 默认、已有 Claude CLI 明确同意后预览、Codex 只检测。检测、生成、接受、撤销分离；不自动读私人历史或发送选区。
- 历史条目可显式放入本地草稿；覆盖已有草稿须确认，长文本拒绝自动截断，载入不会调用模型。
- 前端失联超时反馈、旧响应隔离、中文隐私提示和 UTF-8 字节计数。
- 数字按完整 token 与出现次数双向比较，避免把 `1200 → 12000` 当成不变；检查新增数字、货币符号和英文否定缩写。原文编辑后云端同意失效。
- 修复改写 / 日历卡片被 flex 挤压隐藏按钮；会议搜索区保留最小高度，录音停止条在滚动时可达。
- 有真实内容的会议窄屏列表 / 详情上下排列，工具可换行；说话人改名改为可键盘访问按钮。修复引号、印地语句号、阿拉伯语标点边界的片段间隔。
- 外部进程失败清理不再无界等待；本地模型只接受正常结束的纯文字候选，拒绝截断、工具 / 图片及远端标记。
- 草稿的接受 / 撤销也会重置云端同意，并让旧候选失效；不依赖只有键盘编辑才触发的 input 事件。
- 用虚构长模型名 / 长事件标题复现了检测后下拉框被卡片裁切，补控件最大宽度和状态文字换行；不能只用空下拉框测试布局。

## 已完成检查

| 检查 | 结果及边界 |
| --- | --- |
| 完整 Rust 工作区 / all-targets（17:14 UTC） | 831 项通过，25 项忽略。忽略项包括真实设备、独立模型和显式云端 / 本地集成测试，不算通过；部分既有测试没有特定外部工件会提前返回，不表示签名安装器已验收。 |
| 全部前端合约（17:05 UTC） | 97 项通过；另有社区 UI / 许可证 10 项、Python 打包 9 项通过。模拟宿主，不能代替原生鼠标操作。 |
| 全工作区 clippy `-D warnings`（17:05 UTC） | 通过；旧版 `--no-default-features` 编译检查通过，未安装或更改旧版身份。 |
| 浮条隐藏原生 WebView2 | 9 种布局通过，127% 文字放大；非激活 / 隐藏 / 非任务栏 / 鼠标和触控激活消息检查通过。未实际点按可见浮条。 |
| 旧 Classic / Mini 指示器隐藏原生 WebView2 | 6 种布局通过；修复前确实测到溢出，修复后通过。 |
| 浮条真实 IPC / 隔离控制桥 | 13 种开始 / 双击 / 停止 / 取消 / 陈旧版本 / 隐藏 / 未就绪等场景通过；真实隐藏 WebView 的 DOM 点击发 IPC，但没有连接音频引擎或输入目标。 |
| 完整主界面隐藏原生 WebView2 | 中文、英文各 12 组通过；900×600、700×480、部分 540×360；实际 CSS 视口因文字缩放更小。24 段虚构转录、展开日历与滚动到底部时停止按钮可达。没有录音。以上原生合计 52 个独立场景；17:04 UTC 补长模型名 / 长事件标题后先复现裁切，修复后主窗 24 例于 17:04–17:05 UTC 全部通过，不将重复运行另算新场景。 |
| 浏览器 UI 模拟页 | 中文检测→生成→接受→撤销实际点击通过，草稿恢复与字节数检查通过。页面只用虚构宿主，不连接真实模型。 |
| Claude 2.1.221 / Haiku 合成原文 | 初始安全调用成功；收紧协议后暴露 `system/thinking_tokens` 兼容问题，核对 SDK 数字进度事件并加入明确许可后再次成功，最新一次 27.68 s（含检查）。配置模型曾因 CLI 过旧失败，未升级 CLI 或自动换模型。 |
| Ollama Qwen2.5 7B CPU 合成原文 | 先前一次约 19.8 s；最终 8 样例矩阵全部完成，单条约 7–14 s，详见下节。数字 / 币种格式变化触发保守警告，不能把完成率当成语义准确率。 |
| 源码凭证检查 | Gitleaks 8.30.1 官方工件 SHA-256 已核验；17:26 UTC 交付扫描的源码约 9.66 MB，未发现凭证。检查当前源文件，不代表审计了整个 Git 历史；未扫描个人历史 / 用户目录。 |
| 依赖检查 | Cargo audit 使用 2026-09-23 更新的通告库，0 个已知漏洞；保留 2 个既有维护 / soundness 警告：proc-macro-error 1.0.4、Linux glib 0.18.5。未通过大版本升级改变依赖。 |

所有模型测试原文均为虚构文本。没有上传个人听写、会议、凭证或竞品历史。
Claude CLI 是独立受信程序，不是操作系统沙箱；未知协议、工具、MCP、hook 或不完整结果拒绝接受。
额度状态和数字进度通知不是模型结果，不能作为成功或账单计量依据。

## 语音：第一批 63 次已完成

3 条先前 Fish 合成语音 × 7 种策略 × 3 次重复，顺序轮换；SenseVoice int8 CPU，4 线程。
走生产核心、后台推理工作线程和本地整理器，内存收集输出，**不含实际音频设备或跨应用注入**。
每次先用前三秒暖模型。英文计算 WER，中文计算 CER；参考是生成提示文本，并非人工复听逐字标注。
指标先作 NFKC / 小写归一化并去除标点；中文混合句的英文字母和数字也计入字符单位。它不评分大小写、标点或金额正负号等语义正确性，不能将 `100% − 错误率` 宣称为“理解准确率”；原始输出另行保留。
普通 / 整段模式沿用本地人声检测，渐进模式按现有策略绕过该检测；比较的是这些生产路径，不能解释成只改变一个模型参数的实验。
后台游戏、构建等负载没有控制，以下延迟仅是本机探索，不能用于竞品速度广告。

| 样本 / 策略 | 错误率范围 | 松键后等待中位数 | 首次输出中位数 |
| --- | ---: | ---: | ---: |
| 普通英文 36 s / 整段 | 0% | 1129 ms | 37.17 s |
| 普通英文 / 普通 8 s 预解码 | 0% | 180 ms | 36.23 s |
| 普通英文 / 渐进 0.3 s 下限 | 0% | 88 ms | 3.22 s |
| 英文技术名称 23.6 s / 整段 | 9.52% | 719 ms | 24.29 s |
| 技术名称 / 普通 8 s | 12.70% | 187 ms | 23.76 s |
| 技术名称 / 普通 12 s | 11.11% | 317 ms | 23.89 s |
| 技术名称 / 渐进 0.3 s | 19.05% | 97 ms | 2.82 s |
| 技术名称 / 渐进 3 s | 12.70–15.87% | 107 ms | 4.51 s |
| 中文 34.8 s / 整段 | 0% | 936 ms | 35.73 s |
| 中文 / 普通 8 s | 0.57–1.14% | 90 ms | 34.88 s |
| 中文 / 渐进 0.3 s | 0% | 89 ms | 2.58 s |

结论目前仅是：分段改善松键等待，但技术名词有上下文损失风险；不能一刀切缩短全部语言的片段。
数字是“最小上下文时间”，不是强制定时截断；仍只在停顿处切分。
产品默认值尚未因这张表改变。

## 语音：84 次留出复测已完成

14:18:07–15:13:35 UTC，六段新 Fish 合成语音 × 七种策略 × 两次重复。
两组分段实验使用的探针 SHA-256 为 `02110d7cd40cc8d00784b970353e1710a6cab11d01a2586dde5b94d30e587ca3`。
前两批的实验 JSON 尚未内嵌工件哈希，上述来自运行时的独立记录；复现证据不如后几批完整。最终已补脚本，让未来新实验直接保存探针 / 模型 / 词表 / 参考清单 / WAV 哈希，没有回填或覆盖这两批旧记录。
语言：四段英文、两段中文 / 混合技术词。期间没有改变探针二进制或生产默认值。

| 样本 | 整段错误率 | 普通 8 s | 普通 12 s | 渐进 0.3 s | 渐进 3 s | 渐进 5 s |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 英文产品讨论，56.6 s | 0.58% | 0.58% | 0.58% | 0% | 0% | 0% |
| 英文技术调试，35.2 s | 3.09% | 6.19–7.22% | 5.15–6.19% | 6.19–7.22% | 6.19% | 4.12–6.19% |
| 英文否定 / 约束，33.8 s | 1.04% | 0–1.04% | 0% | 1.04% | 1.04% | 0–1.04% |
| 英文自我修正，30.6 s | 0% | 0% | 0% | 0% | 0% | 0% |
| 中文产品讨论，39.2 s | 0% | 0% | 0% | 0% | 0% | 0% |
| 中文混合技术词，32.5 s | 2.56% | 2.56% | 2.56% | 1.54% | 2.56% | 2.56–3.08% |

解读与决定：

- 更长上下文对部分英文技术词有帮助，但不是单调变好：中文混合术语在更长渐进片段中退步，且首次输出更迟。不根据这九条合成样本一刀切改全语言默认值。
- 英文技术样本的 `WebSocket` 错写是真正需要改进的例子；`frontend → front end`、`cancelled → canceled` 也会增加当前词错误率，但不能都解释成意思错了。原始指标保留，不为降低分数加入针对样本的替换。
- 较早的英文名称样本仍出现 `PostgreSQL`、`TypeScript`、`Collie` 等错误；这说明不能只靠改停顿阈值解决专业词。合成语音本身是否准确发出了每个专名，尚无人工逐字标注确认。
- 普通模式只在松键后交付，后台预解码不是“边说边输入”。渐进模式只追加已确认片段；内部收集器匹配不证明某个原生文本框已收到文字。
- 下一组控制语言提示、线程数；五分钟拼接音频用于队列与长时间运行压力，不算新增独立自然语音样本。

## 语音：72 次语言提示 / CPU 线程对照已完成

15:15:46–16:03:31 UTC；六条留出音频 × 六种设置 × 两次，使用整段识别隔离分段因素。
探针 SHA-256：`bd036d3e5d01257a0cf5262b80905b2fdec8844dc021be7679f9a553fda33709`。
CPU 为 AMD Ryzen 9 9950X3D（16 核 / 32 逻辑线程），系统报告内存总量约 61.68 GiB；后台负载未控制。

| 样本 | auto / 1 线程 | auto / 2 线程 | auto / 4 线程 | auto / 8 线程 |
| --- | ---: | ---: | ---: | ---: |
| 英文产品讨论 56.6 s | 2222 ms | 1301 ms | 907 ms | 769 ms |
| 英文技术调试 35.2 s | 1162 ms | 666 ms | 431 ms | 357 ms |
| 英文否定 / 约束 33.8 s | 1102 ms | 623 ms | 403 ms | 337 ms |
| 英文自我修正 30.6 s | 969 ms | 563 ms | 345 ms | 294 ms |
| 中文产品讨论 39.2 s | 1329 ms | 787 ms | 512 ms | 406 ms |
| 中文混合术语 32.5 s | 1065 ms | 603 ms | 376 ms | 302 ms |

表为暖模型、松键后的整段结果等待中位数，不含实际输入，亦不是五分钟语音的推算。

- 固定 4 线程，四条英文的 `auto / zh / en` 提示错误率完全相同；这批样本**不支持**“中文语言提示导致英文变慢 / 不准”的归因。
- 中文产品讨论均为 0% CER；混合术语在 auto 下为 3/195（1.54%），zh / en 为 5/195（2.56%），两次一致。但只有一条混合样本，不据此全局改中文默认。
- 本机从 4 到 8 线程，六条的中位数改善约 15–21%，没有翻倍；线程和后台争用的开销仍需低端 CPU / 笔记本验证。生产默认未改。
- 这批探针尚无进程内存 / CPU 时间字段；不能用墙钟等待冒充 CPU 消耗。后续压力探针才新增自身资源记录。
- 本机没有现成 Parakeet / Whisper 权重，未自动下载。候选及同条件评估方案见 [英文 CPU 下一轮计划](ENGLISH-CPU-NEXT.zh-CN.md)。

## 语音：五分钟长压力 9 次已完成

16:05:02–16:52:45 UTC。四条英文留出音频各重复两遍，间隔 600 ms 静音，合成一条 **316.763 秒**的压力音频。
这不是新录制的自然对话，也不算增加了八个独立准确率样本。每种策略三次，顺序轮换；真实时间推进音频，不加速回放。
探针 SHA-256：`bca408af7e24b766f8b83d7ecc56048e00a6936ef00f56bb2bce4e2a890316f7`。

| 策略 | WER 范围 | 松键后中位数 | 首输出中位数 | 整个探针累计 CPU 中位数 | 峰值工作集范围 | 峰值私有提交范围 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 普通 8 s 预解码 | 1.62–1.73% | 170 ms | 316.93 s | 22.75 s | 389.1–389.7 MiB | 402.9–403.5 MiB |
| 渐进 0.3 s 下限 | 1.52% | 130 ms | 3.21 s | 30.48 s | 376.9–395.1 MiB | 377.7–410.6 MiB |
| 渐进 3 s 下限 | 1.30–1.52% | 132 ms | 3.21 s | 27.16 s | 375.8–378.3 MiB | 376.6–378.7 MiB |

- 9/9 次内部追加片段拼接与最终文本完全一致；没有崩溃或回放超时。它不证明原生输入框已接收到文字。
- 普通模式首输出在停止后，这是预期交付语义；渐进模式较早输出，但累计 CPU 用量更高。不能把更低松键等待理解为更低总计算成本。
- 资源是 Windows 对**该探针进程全程**的计量，含加载 / 三秒暖机 / 音频推进与解码，CPU 是各线程时间总和，不是墙钟占用率；私有提交也不等于常驻 RAM。没有测量或汇总其他应用进程。
- 只测带停顿的分段生产路径，没有为五分钟整段注意力分配做压力冒险；长时间连续无停顿仍可能在松键后集中处理。
- 非受控后台负载和分段推进时机使重复结果有变化，因此保留范围，而非挑最好的一次。没有修改模型权重、阈值或生产片长默认值。

## 语音：4 / 8 线程资源对照 24 次已完成

16:52:59–17:08:54 UTC。六条留出样本 × 两种线程数 × 两次，第二次调换先后次序。
语言提示固定 auto、整段暖模型识别；使用与长压力相同的资源探针，期间未更换二进制。

| 样本 | 等待中位数：4 → 8 线程 | 探针全程 CPU 中位数：4 → 8 线程 |
| --- | ---: | ---: |
| 英文产品讨论 | 1358 → 1021 ms | 6.51 → 9.36 s |
| 英文技术调试 | 679 → 538 ms | 3.89 → 5.54 s |
| 英文否定 / 约束 | 660 → 474 ms | 3.80 → 5.18 s |
| 英文自我修正 | 570 → 406 ms | 3.45 → 4.52 s |
| 中文产品讨论 | 830 → 579 ms | 4.48 → 5.85 s |
| 中文混合技术词 | 614 → 429 ms | 3.63 → 4.87 s |

- 这批 4 → 8 线程等待中位数改善约 **21–30%**，全程累计 CPU 增加约 **31–44%**；后者不是功耗或电池续航测量。
- 六条样本错误率均未因线程数变化：英文分别 0.58%、3.09%、1.04%、0%；中文 0%、1.54%。两次重复相同。
- 峰值工作集总范围约 396–488 MiB，峰值私有提交约 414–521 MiB；不同音频长度不能合并成单一的产品 RAM 占用承诺。
- 与上一批 72 次的绝对延迟不同，说明后台负载 / 运行时条件确实影响数字。没有把两批时间混成一个受控基准，也没有拿它与竞品宣传数字相比。
- 本轮合计 **252 次合成音频回放**：63 + 84 + 72 + 9 + 24。重复不是新增独立样本；独立语音只有九条，另有一条由其中四条拼接的压力音频。
- 结论是保留默认设置，后续在低端 CPU / 笔记本上考虑显式性能档位或可撤销建议，不能仅根据逻辑核心数量静默调高线程。

## 本地文本改写：8 个虚构样例

17:09–17:10 UTC，本机已安装的 Ollama `qwen2.5:7b-instruct`，请求 CPU / 最多 4 线程 / 4096 上下文 / 512 输出上限，逐次请求卸载模型。
测试后本地服务报告该标签 digest 为 `845dbda0ea48ed749caafd9e6037047aa19acfcfd82e704d7ca97d631a0b697e`、大小 4,683,087,332 字节；这是服务的模型清单标识，不是独立对运行时的安全审计。
八次顺序完成，测试本身 69.47 秒；下表为 `/api/chat` 等待，不含 UI 操作。本轮没有新下载、云端回退或真实原文上传。

| 样例 | 操作 | 等待 | 人工查看到的边界 |
| --- | --- | ---: | --- |
| 英文金额 / 否定 | 轻润色 | 9.99 s | 保留未批准 / 不部署，但 `1200 USD`、`12000 USD` 改成 `$1,200`、`$12,000`；币种限定和格式变化被警告，不能自动当作完全等价 |
| 英文提议 / 日程 | 列表 | 8.03 s | 保留 Wednesday 14:00、提议未确定、未同意部署；没有把提议写成决定 |
| 中文金额 / 否定 | 轻润色 | 7.58 s | 保留 1200 / 12000 USD、Collie、未批准和暂不部署 |
| 中文总结 | 摘要 | 7.05 s | 保留生产 / 测试环境区别和未批准；“没有”改“未”等近义变化仍触发保守提示 |
| 日文名称 / 提议 | 轻润色 | 7.44 s | 本例文字未变；保留金额和未批准 / 未确定 |
| 韩文金额 / 否定 | 轻润色 | 8.41 s | 保留否定；1200 改 1,200，系统提示检查格式变化 |
| 印地语名称 / 提议 | 轻润色 | 13.84 s | 本例保留 Collie、1200 USD、未批准和会议未确定 |
| 英文带引号的指令文字 | 轻润色 | 7.07 s | 仍是候选文字，引用格式变化；未执行文字里的操作，适配器不提供工具执行能力 |

8/8 指**请求和结果协议完成**，不是 100% 语义准确率；日 / 韩 / 印地语没有母语者盲评。这里是文本改写测试，不是这几种语言的语音识别测试。
没有警告也不表示含义一定没变；人工编辑候选后同样需要重新核对。由此保留手动预览 / 接受 / 撤销，不接入每次听写的自动替换。
原始虚构输入、输出与警告保存在本机 `rewrite-local-matrix.jsonl`；不包含私人历史。

## 人声检测补充回归（不是去噪或说话人识别）

17:12 UTC，用已有 `speech_gate_probe` 和本地固定哈希的 Silero v5 检测模型：

- 四个三秒合成负样本：静音、白噪声、60 Hz 嗡声、点击声，均为 `NoSpeech`。
- 六条中英文留出语音，分别原音量、降低 20 dB、叠加低幅白噪声，18/18 为 `Speech`。
- 每条语音之后再测静音，6/6 为 `NoSpeech`，没有把上一条检测状态带入下一条。
- 共 28 个检测决定。加载约 80 ms；语音通过通常在早期检测到，不表示逐帧检查整段只用了约 1 ms，也不是 ASR 准确率成绩。

未修改阈值。这些不是现实房间、电视人声、音乐或重叠说话的验收；模型不能判断哪一个人是用户。
小于 750 ms 的显式听写会保守放行以保留短回答，渐进输入也沿用现有绕过规则，因此不能承诺所有短噪声都被拦住。

## 本地构建记录

- 最终 `cargo build --locked -p vocalcode-app --features community --release` 于 17:11 UTC 完成。
- 工件：`C:\workspace\vocalcode-community-build-20260922\release\vocalcode-app.exe`，11,999,744 字节；SHA-256：`7440e1abd037c18b271f9dfa2e3e317093164f66b4168783bd66dfcb899b9725`。
- 后续兼容旧安装的构建复用了 target 输出路径；上述社区版工件及四个 DLL 已先归档到 `C:\workspace\vocalcode-product-review-20260923\community-build-before-local-replace`，并核验同一 SHA-256。
- 仅执行早期只读 `--build-info`，确认 `edition=community`、`version=1.3.1`、独立 `VocalCode Community` 数据目录和社区更新渠道；没有启动 GUI / 麦克风 / 模型下载。
- 四个所需本地运行时 DLL 存在，身份检查能运行。不代表签名安装器或跨机器安装已验收。
- 没有增加版本号、签名、安装或发布。这是带新改动的未发布开发构建，不是已经给用户推送的“新版”。
- 17:12 UTC 当前已安装旧版的 SHA-256 仍为 `b580d1c81b74dd1e8bc0fbeb4581d4fdbc68551c481b77d82198886d7c1890b9`，与本轮开始记录一致。未迁移个人模型 / 词典 / 历史。
- 两个本轮 UI-only 预览服务已按精确进程身份关闭；没有关闭用户应用，也没有递归删除测试或个人数据。

## 复现入口

- `cargo test --workspace --all-targets --locked --features vocalcode-app/community -- --quiet`
- `cargo clippy --workspace --all-targets --locked --features vocalcode-app/community -- -D warnings`
- `node --test packaging/release/test_*.mjs`（PowerShell 可先展开文件列表）
- `cargo run -p vocalcode-app --example control_bar_hidden_smoke`
- `cargo run -p vocalcode-app --example control_bar_ipc_smoke`
- `cargo run -p vocalcode-app --example indicator_hidden_smoke`
- `cargo run -p vocalcode-app --example webui_hidden_smoke -- zh` / `-- en`
- `packaging/release/benchmark_segment_context.mjs`、`benchmark_decode_settings.mjs`、`benchmark_long_stress.mjs`、`summarize_replay.mjs`

本机合成音频与原始实验产物：`C:\workspace\vocalcode-product-review-20260923`。
不提交二进制模型、音频或机器上的 WebView 用户目录。各测试专属临时目录保留以便诊断，没有递归清理用户数据。

## 仍需验收 / 不作承诺

- 可见浮条的真实鼠标 / 触控、Win32 / Electron / WebView2 输入目标和混合 DPI 多屏。隐藏测试不能代替这些。
- macOS 原生编译、权限、窗口与 CLI；本轮不虚报为已测。
- 真正双人会议、扬声器回声、设备断开、长时间录音和真人口音；本轮只测合成样本及已有逻辑测试。
- 本地 LLM 改写与随后新听写可能争用 CPU；模型文件上限不是 RAM 上限。
- 无法可靠禁用工具的 Codex CLI 不执行；不自动修复 / 登录 / 升级外部工具。
- 本地 / 外部模型标签与 CLI 协议会变化；此次通过只适用于记录的本机版本，错误时不会自动升级或切换服务。
- 最早两批分段实验缺少内嵌哈希，后续脚本修复不追溯伪造旧实验的完整性。
